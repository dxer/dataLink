package io.dxer.etl

import org.apache.spark.sql.catalyst.parser._


object SparkSQLTest {
  def main(args: Array[String]): Unit = {
//    val sqlText = "select 222 rewrwe  from test"
//
//    val parser = new SparkSQLChecker(null)
//
//
//    val start = System.currentTimeMillis()
//    parser.check(sqlText)
//    parser.check(sqlText)
//    parser.check(sqlText)
//    parser.check(sqlText)
//    val end = System.currentTimeMillis()
//    println(s"ssss ${end - start}")
  }
}
